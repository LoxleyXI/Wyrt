# Wyrt Server Environment Variables
# Copy this file to .env and configure your values
# DO NOT commit .env to git (it contains secrets)

# ============================================
# OAuth Configuration
# ============================================
# OAuth credentials are configured per-game module, NOT at the engine level.
# Each game module has its own .env file with OAuth credentials.
#
# See:
#   modules/lairs/.env - Lairs platform OAuth
#   modules/fyrnvale/.env - Fyrnvale OAuth
#
# This allows each game to have its own Discord app with appropriate branding.

# ============================================
# Database Configuration
# ============================================

# MySQL/MariaDB connection
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=wyrt

# ============================================
# Server Configuration
# ============================================

# WebSocket port (default: 8080)
# WS_PORT=8080

# HTTP API port (default: 4040)
# HTTP_PORT=4040

# CORS origins (comma-separated, default allows localhost)
# CORS_ORIGINS=http://localhost:8000,http://localhost:8001,http://localhost:3000
