{
  "name": "wyrt_data",
  "version": "1.0.0",
  "description": "Generic game data schema for Wyrt games",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "migrate:my_game": "npx tsx scripts/migrate-my_game.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
